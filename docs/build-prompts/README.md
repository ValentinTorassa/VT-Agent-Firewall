# Build prompts

The prompts used to scaffold the first version of this repository with an AI
coding agent (Kimi Code), kept for transparency. They are not part of the
gateway and nothing imports them. Order: `A*` (problem, threat model, contract),
`B1` (scaffold), `R*` (hardening rounds).
